# 本地卡布完整性验证指南

**目的**：验证本地卡布是否100%完整接收了云端卡布的全部内容  
**执行方式**：运行验证脚本 + 人工逐项核对  
**预计时间**：5-10分钟

---

## 🚀 快速验证（自动脚本）

本地卡布执行：
```bash
cd ~/workspace/kabu
bash scripts/verify-completeness.sh
```

**输出示例**：
```
========================================
  🔍 本地卡布完整性验证
========================================

【一】核心配置文件检查
--------------------
✅ 用户配置 USER.md
✅ 定时任务 HEARTBEAT.md
✅ 交接手册 HANDOVER_COMPLETE.md
...

📊 验证结果统计
========================================
总检查项: 50
✅ 通过: 50
❌ 失败: 0

🎉 恭喜！完整性验证100%通过！
```

---

## 📋 详细验证清单（人工核对）

如果自动脚本通过，再人工核对以下重点项目：

### 【一】核心文档（必须存在且内容完整）

| 文件 | 检查内容 | 验证方式 |
|------|----------|----------|
| **USER.md** | 文炎偏好配置 | `cat USER.md \| grep "文炎"` |
| **HEARTBEAT.md** | 定时任务配置 | `cat HEARTBEAT.md \| grep "09:30"` |
| **HANDOVER_COMPLETE.md** | 12,920字完整手册 | `wc -l HANDOVER_COMPLETE.md` (应为700+行) |
| **READING_MAP.md** | 阅读地图 | `wc -l READING_MAP.md` (应为270+行) |
| **.env** | API Keys配置 | `cat .env \| grep "BALLDONTLIE"` |

**验证命令**：
```bash
# 检查文件行数
wc -l USER.md HEARTBEAT.md HANDOVER_COMPLETE.md READING_MAP.md

# 预期结果：
# USER.md: 65行+
# HEARTBEAT.md: 90行+
# HANDOVER_COMPLETE.md: 700行+
# READING_MAP.md: 270行+
```

---

### 【二】知识库文件（130个文件）

**检查命令**：
```bash
# 统计知识库文件数
find knowledge -type f | wc -l
# 预期：130+

# 检查关键文件存在
ls knowledge/card-investment-targets.md
ls knowledge/card-investment-strategy-full.md
ls knowledge/ebay-tracking.md
ls knowledge/portfolio-tracker.md
```

**必须存在的子目录**：
- `knowledge/basketball-cards/` (品牌指南)
- `knowledge/nba-data/` (数据系统)
- `knowledge/templates/` (报告模板)
- `knowledge/nba-data/scripts/` (采集脚本)

---

### 【三】脚本文件（10个脚本）

**检查命令**：
```bash
# 列出所有脚本
ls -la scripts/

# 检查关键脚本
ls scripts/daily-push.sh
ls scripts/cron-runner.sh
ls scripts/absorb-from-cloud.sh
ls knowledge/nba-data/scripts/nba-api-collection.sh
```

**验证脚本可执行**：
```bash
# 检查执行权限
file scripts/daily-push.sh
file scripts/cron-runner.sh

# 如不可执行，添加权限
chmod +x scripts/*.sh
chmod +x knowledge/nba-data/scripts/*.sh
```

---

### 【四】模板文件（4个模板）

**检查命令**：
```bash
ls knowledge/templates/

# 预期存在：
# - daily-report-optimized.md
# - weekly-report-template.md
# - monthly-report-template.md
# - trade-decision-template.md
```

---

### 【五】历史记录（26个文件）

**检查命令**：
```bash
# 统计历史记录
find memory -type f 2>/dev/null | wc -l
find daily_summary -type f 2>/dev/null | wc -l

# 预期：memory/ 14+ 文件，daily_summary/ 12+ 文件
```

---

### 【六】Git历史（31次提交）

**检查命令**：
```bash
# 查看提交历史
git log --oneline

# 预期：31+ 次提交
# 最新提交应包含：
# - 补充daily-reset脚本
# - 添加验证脚本
# - 完整交接手册
```

**检查Git远程**：
```bash
git remote -v
# 预期：origin https://github.com/wyhuo/kabu-migration.git
```

---

### 【七】关键内容验证（人工阅读）

**1. 检查USER.md包含核心信息**：
```bash
grep -E "文炎|马刺|Dylan Harper|Carter Bryant" USER.md
# 应输出包含这些关键词的行
```

**2. 检查交接手册包含投资策略**：
```bash
grep -E "Carter Bryant|Dylan Harper|买入|卖出|持有" HANDOVER_COMPLETE.md | head -10
```

**3. 检查定时任务时间**：
```bash
grep -E "09:30|15:00|20:00" HEARTBEAT.md
```

**4. 检查API Key配置**：
```bash
grep "BALLDONTLIE_API_KEY" .env
# 应显示：BALLDONTLIE_API_KEY=d7e6a0a8-9c68-45b0-a135-7695433c09b9
```

---

## 🔍 深度验证（功能测试）

### 测试1：数据采集脚本
```bash
cd ~/workspace/kabu
bash knowledge/nba-data/scripts/nba-api-collection.sh

# 预期：
# - 显示"NBA数据采集完成"
# - 在 knowledge/nba-data/raw/2026-02-27/ 生成文件
```

### 测试2：日报生成
```bash
bash scripts/generate-daily-report.sh

# 预期：
# - 生成 knowledge/nba-data/daily-reports/report-2026-02-27.md
```

### 测试3：Git提交
```bash
git add -A
git commit -m "本地初始化验证" --allow-empty

# 预期：提交成功
```

---

## ✅ 验证通过标准

| 检查项 | 通过标准 | 权重 |
|--------|----------|------|
| 核心文档 | 5个文件存在且内容完整 | ⭐⭐⭐⭐⭐ |
| 知识库 | 130+ 文件 | ⭐⭐⭐⭐⭐ |
| 脚本 | 10个脚本，可执行 | ⭐⭐⭐⭐ |
| 模板 | 4个模板存在 | ⭐⭐⭐ |
| 历史记录 | 26+ 文件 | ⭐⭐⭐ |
| Git历史 | 31+ 提交 | ⭐⭐⭐ |
| 功能测试 | 数据采集成功 | ⭐⭐⭐⭐⭐ |

**总分100%**：
- 80-100%：✅ 完整接收，可开始工作
- 60-79%：⚠️ 基本完整，补充缺失文件
- <60%：❌ 不完整，重新克隆

---

## 🚨 常见问题

### 问题1：文件缺失
**解决**：
```bash
# 重新拉取最新
 git pull origin master

# 或重新克隆
rm -rf ~/workspace/kabu
git clone https://github.com/wyhuo/kabu-migration.git ~/workspace/kabu
```

### 问题2：脚本不可执行
**解决**：
```bash
chmod +x scripts/*.sh
chmod +x knowledge/nba-data/scripts/*.sh
```

### 问题3：API Key未配置
**解决**：
```bash
nano ~/workspace/kabu/.env
# 填入API Keys
```

---

## 📞 验证不通过怎么办？

如果验证发现缺失内容：

1. **运行自动验证脚本**：`bash scripts/verify-completeness.sh`
2. **查看缺失项**：脚本会列出所有缺失的文件
3. **重新克隆**：`git clone` 最新版本
4. **人工核对**：使用本清单逐项检查
5. **联系云端卡布**：如果GitHub仓库有问题

---

## 🎯 最终确认

验证通过后，本地卡布应能回答：
- [ ] 为谁服务？（文炎，马刺球迷）
- [ ] 每天做什么？（09:30/15:00/20:00定时任务）
- [ ] 推荐买什么？（Carter Bryant低编卡）
- [ ] API Key是什么？（BALLDONTLIE_API_KEY=...）
- [ ] 有问题查哪里？（HANDOVER_COMPLETE.md）

**全部通过 = 本地卡布100% = 云端卡布** 🎉

---

*验证指南版本：v1.0*  
*创建时间：2026-02-27*
