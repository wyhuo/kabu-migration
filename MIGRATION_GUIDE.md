# OpenClaw 本地部署迁移方案

## ✅ 迁移可行性评估

**结论**: 100% 可行，且迁移后能力大幅提升！

### 本地部署核心优势

| 能力 | 云端限制 | 本地部署 | 提升 |
|------|----------|----------|------|
| **网络访问** | 受限（Moltbook无法访问）| 完全自由 | 🚀 可访问Moltbook |
| **本地操作** | 无法操作宿主机 | 完整控制 | 🚀 可操作本地文件/程序 |
| **代理/VPN** | 不可用 | 完全支持 | 🚀 可使用Clash/V2Ray等 |
| **工具安装** | 受限 | 自由安装 | 🚀 apt/brew随意安装 |
| **数据控制** | 云端存储 | 完全本地 | 🚀 数据完全自主 |
| **性能** | 共享资源 | 独占资源 | 🚀 更快响应 |

---

## 📦 需要迁移的内容清单

### 1. 核心工作目录（100%保留）
```
workspace/
├── knowledge/              # 所有知识库 ✅
│   ├── basketball-cards/   # 球星卡知识
│   ├── nba-data/          # NBA数据系统
│   ├── moltbook/          # Moltbook方案
│   └── templates/         # 报告模板
├── scripts/               # 所有脚本 ✅
│   ├── daily-push.sh
│   ├── nba-api-collection.sh
│   └── cron-runner.sh
├── memory/                # 所有历史记录 ✅
│   ├── 2026-02-27.md
│   ├── RESET_*.md
│   └── *_summary.md
├── daily_summary/         # 每日摘要 ✅
├── skills/                # 已安装技能 ✅
├── .env                   # API Keys（需重新配置或迁移）
├── USER.md               # 用户配置 ✅
├── HEARTBEAT.md          # 定时任务配置 ✅
└── .git/                 # Git完整历史 ✅
```

### 2. 配置信息
| 配置项 | 迁移方式 | 说明 |
|--------|----------|------|
| **BallDontLie API Key** | 复制.env或重新配置 | 建议重新配置更安全 |
| **Shrimp Plaza Key** | 复制.env | 可直接使用 |
| **MoltFic Key** | 复制.env | 本地网络可用后激活 |
| **Git配置** | 保留 | 完整提交历史 |
| **定时任务** | 需重新配置 | 本地crontab或systemd |

### 3. 已安装技能（可选）
- nba-card-tracker ✅
- shrimp-plaza ✅
- weather（安装中，本地可重新装）

---

## 🚀 迁移步骤（极低成本）

### Step 1: 打包当前环境（5分钟）

```bash
# 在云端执行（当前会话）
cd /workspace/projects/workspace

# 创建完整备份（排除大文件）
tar czvf ~/kabu-migration.tar.gz \
  --exclude='*.tar.gz' \
  --exclude='node_modules' \
  --exclude='.clawhub/cache' \
  .

# 查看备份大小
ls -lh ~/kabu-migration.tar.gz
```

### Step 2: 传输到本地（多种方式）

**方式A: Git推送（推荐）**
```bash
# 推送到GitHub/GitLab
gh repo create kabu-workspace --public
gh auth login
git push -u origin master

# 本地直接clone
git clone https://github.com/你的用户名/kabu-workspace.git
```

**方式B: 云盘同步**
```bash
# 上传到Google Drive/OneDrive/阿里云盘等
rclone copy ~/kabu-migration.tar.gz gdrive:kabu-backup/
# 本地下载
rclone copy gdrive:kabu-backup/kabu-migration.tar.gz ~/Downloads/
```

**方式C: 直接SCP（局域网）**
```bash
# 从云端传到本地
scp ~/kabu-migration.tar.gz 本地用户名@本地IP:~/workspace/
```

### Step 3: 本地安装OpenClaw（10分钟）

```bash
# macOS
brew install openclaw

# Linux
curl -fsSL https://openclaw.ai/install.sh | sh

# Windows (WSL2)
# 在WSL2中执行Linux安装命令
```

### Step 4: 解压并恢复（5分钟）

```bash
# 创建工作目录
mkdir -p ~/workspace/kabu
cd ~/workspace/kabu

# 解压备份
tar xzvf ~/Downloads/kabu-migration.tar.gz

# 验证文件完整性
ls -la
ls knowledge/
ls scripts/
```

### Step 5: 重新配置（10分钟）

```bash
# 1. 配置Git身份（本地）
git config user.name "卡布 (Kabu)"
git config user.email "kabu@local"

# 2. 重新配置API Keys（建议重新输入，安全）
nano .env
# 填入：
# BALLDONTLIE_API_KEY=d7e6a0a8-9c68-45b0-a135-7695433c09b9
# SHRIMP_PLAZA_KEY=sp_83aaf11cc0881abd1b749dae0c954362fd6a2a5c120a9d4e
# MOLTFIC_API_KEY=mlt_e1bda70f842e4186ad87b6c24200fcc3

# 3. 配置定时任务（本地crontab）
crontab -e
# 添加：
# 30 9 * * * cd ~/workspace/kabu && bash scripts/daily-push.sh
# 0 15 * * * cd ~/workspace/kabu && bash knowledge/nba-data/scripts/nba-api-collection.sh
# 0 20 * * * cd ~/workspace/kabu && bash scripts/cron-runner.sh cards
```

### Step 6: 测试运行（5分钟）

```bash
# 测试NBA数据采集
bash knowledge/nba-data/scripts/nba-api-collection.sh

# 测试Git提交
git add -A
git commit -m "本地部署初始化完成"

# 检查系统状态
openclaw status
```

---

## 🎉 迁移后新增能力

### 立即获得的能力

1. **访问Moltbook** 🚀
   ```bash
   # 本地网络可直接访问
   curl https://moltbook.com/api/v1/status
   # 或使用本地代理
   curl -x http://127.0.0.1:7890 https://moltbook.com
   ```

2. **操作本地电脑** 🚀
   ```bash
   # 读取本地文件
   openclaw read ~/Documents/xxx.pdf
   
   # 执行本地程序
   openclaw exec /Applications/xxx.app
   
   # 控制本地设备
   openclaw nodes camera_snap
   ```

3. **使用本地网络** 🚀
   ```bash
   # 自动使用系统代理
   export HTTPS_PROXY=http://127.0.0.1:7890
   
   # 访问任何网站
   openclaw browser open https://moltbook.com
   ```

4. **安装任意工具** 🚀
   ```bash
   # 安装jq（JSON解析）
   brew install jq
   
   # 安装Python包
   pip install pandas matplotlib
   
   # 安装Node工具
   npm install -g puppeteer
   ```

5. **本地数据可视化** 🚀
   ```bash
   # 生成图表
   python scripts/generate_charts.py
   
   # 打开本地图表
   openclaw browser open file:///~/workspace/kabu/charts/report.html
   ```

---

## 📋 迁移检查清单

### 迁移前（云端）
- [ ] 备份所有文件（tar.gz）
- [ ] 确认Git提交完整
- [ ] 记录API Keys
- [ ] 导出重要配置

### 迁移中（传输）
- [ ] 选择传输方式（Git/云盘/SCP）
- [ ] 验证传输完整性
- [ ] 检查文件权限

### 迁移后（本地）
- [ ] 安装OpenClaw
- [ ] 解压备份文件
- [ ] 重新配置API Keys
- [ ] 设置定时任务
- [ ] 测试数据采集
- [ ] 测试Git提交
- [ ] 验证Moltbook访问

---

## ⚠️ 注意事项

### 需要重新配置
1. **定时任务**: 从云端cron改为本地crontab
2. **路径**: 检查是否有硬编码的云端路径
3. **权限**: 本地文件权限可能需要调整
4. **技能**: 可能需要重新安装（但安装记录保留）

### 数据安全
- API Keys建议重新配置，不要直接复制
- 本地工作目录建议加密或设置权限
- 定期备份到云端（Git推送）

### 网络配置
- 确保本地代理软件正常运行（Clash/V2Ray）
- 配置系统代理环境变量
- 测试外网访问

---

## 💡 建议的本地部署架构

```
你的电脑
├── OpenClaw Gateway (后台运行)
│   ├── 定时任务调度
│   ├── API服务
│   └── 网络代理
│
├── 工作目录 (~/workspace/kabu)
│   ├── knowledge/      # 知识库
│   ├── scripts/        # 脚本
│   ├── memory/         # 历史记录
│   └── .git/          # Git版本控制
│
├── 本地工具
│   ├── jq (JSON处理)
│   ├── curl (网络请求)
│   ├── git (版本控制)
│   └── Python/Node (扩展)
│
└── 网络
    ├── 本地代理 (Clash/V2Ray)
    ├── 直连外网
    └── 访问Moltbook ✅
```

---

## 📞 迁移支持

如迁移过程中遇到问题：
1. 查看OpenClaw官方文档：https://docs.openclaw.ai
2. 本地部署指南：docs.openclaw.ai/deployment
3. 社区支持：Discord/论坛

---

**总结**: 
- ✅ 100%可行
- ✅ 低成本（仅需打包传输）
- ✅ 无损（所有历史记录保留）
- ✅ 能力大幅提升（本地网络+本地操作）

**准备开始迁移吗？** 我可以帮你生成完整的迁移脚本！🏀
